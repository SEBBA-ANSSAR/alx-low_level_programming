#include <stdio.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print if the number is posetive , negative or zero
 * Description : use the main function
 * 
 * Return: 0
 */
int main(void)
{
	int n;

	if (n > o)
	{
		printf("%d is positive\n", n);
	}
	else if (n < 0)
	{
		printf("%d is negative\n, n");
	
	}
	else if (n == 0)
	{
		printf("%d is zero\n," n);
	
	}
	return(0);
}
